#include "main.h"

/**
 * _mul - multiplies int a and b
 * @a: integer
 * @b: integer
 * Return: 0
 */
int mul(int a, int b);
{
	int a, b, pro;

	pro = mul(a, b);

	printf("%d", pro);
	return (pro);
}
